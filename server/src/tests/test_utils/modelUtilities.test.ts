// import { validateURL } from "../../utils/modelUtilities";

describe("validateURL", () => {
    it.skip("Should return true for valid URL", () => {
        // expect(validateURL("https://www.youtube.com/watch?v=Oe421EPjeBE&t=26486s")).toBe(true);
    });
    it.skip("Should return false for invalid URL", () => {
        // expect(validateURL("hacker@me.com")).toBe(false);
    });
    it.skip("Should return false for invalid URL", () => {
        // expect(validateURL("io")).toBe(false);
    });
    it.skip("Should return true for valid URL", () => {
        // expect(validateURL("http://www.guc.edu.eg")).toBe(true);
    });
    it.skip("Should return true for valid URL", () => {
        // expect(validateURL("www.facebook.com")).toBe(true);
    });
});
